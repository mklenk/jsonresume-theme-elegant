
unless _.isEmpty(resume.publications)
  .detail#publications
    +large_icon('newspaper', 'Veröffentlichungen')
    .info
      h4.title.text-uppercase Veröffentlichungen

      .content
        ul.list-unstyled.clear-margin
          each publication in resume.publications
            li.card.card-nested
              .content
                p.clear-margin
                  strong
                    if publication.website
                      a(href=publication.website, target="_blank")= publication.name
                      |&nbsp;,&nbsp;
                    else
                      |#{publication.name + ', '}
                  |#{publication.publisher}

                p.text-muted
                  if publication.releaseDate
                    small= 'Veröffentlich am: ' + publication.releaseDate
                  else
                    small= 'TBA'

                if publication.summary
                  .mop-wrapper!= publication.summary
